CREATE TABLE public.locations
(
    id_location SMALLSERIAL PRIMARY KEY,

    code VARCHAR(30) NOT NULL,

    name VARCHAR(100) NOT NULL,

    description VARCHAR(250),

    active BOOLEAN NOT NULL DEFAULT TRUE,

    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),

    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),

    CONSTRAINT uq_locations_code
        UNIQUE (code),

    CONSTRAINT ck_locations_code_not_empty
        CHECK (BTRIM(code) <> ''),

    CONSTRAINT ck_locations_name_not_empty
        CHECK (BTRIM(name) <> '')
);


INSERT INTO public.locations
(
    code,
    name,
    description
)
VALUES
    ('CLEANING', 'Cleaning Room', 'Área de limpieza y resguardo de herramental'),

    ('PISO', 'Piso de Producción', 'Herramental actualmente en producción'),

    ('CUARTERIAS', 'Cuarterías', 'Área de almacenamiento de charolas'),

    ('REPARACION', 'Reparación', 'Herramental enviado a reparación'),

    ('SCRAP', 'Scrap', 'Herramental fuera de uso o dado de baja');